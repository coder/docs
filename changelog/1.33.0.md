---
title: "1.33.0"
description: "Released on 07/27/2022"
---

### Breaking changes ❗

- infra: the "Getting default user from image" build step now spawns a container
  that consumes 100m CPU and 250mb of memory. Previously these were unset, which
  can cause issues with some Kubernetes variants.

### Features ✨

- web: allow multiple links to the same OAuth service type
- cli: allow local address specification in `coder tunnel`
- cli: add retry logic to `coder tunnel`. Default is 0 retries.
- infra: add the ability to set annotations on the `environments` service account.

### Bug fixes 🐛

- infra: increase terminal timeout to 15 minutes.
- infra: move the quota check in builds to a build step
- infra: fix dotfiles validation to allow extra formats
- infra: fix some websocket behavior on webkit-based browsers
- infra: set memory/cpu limits in inner container for CVMs
- infra: fix an issue where coderd would not launch if OIDC config errored

### Security updates 🔐

There are no security updates in 1.33.0.
