Images used with Coder require **glibc**.

Additionally, the use of [SSH](doc:access-an-environment-via-ssh-2) also
requires:

* OpenSSH server
* bash
* curl or wget

For in-depth details (as well as the requirements for specific use cases),
please see Microsoft’s [Linux Prerequisites for Visual Studio Code Remote
Development](https://code.visualstudio.com/docs/remote/linux) doc.
